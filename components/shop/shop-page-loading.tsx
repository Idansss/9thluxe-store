import { RouteLoading } from "@/components/loading/route-loading"

/** Kept for any direct imports; mirrors the shop route loader. */
export function ShopPageLoading() {
  return <RouteLoading label="Curating the collection" />
}
